# Eva — опциональные зависимости (ставить только при необходимости).
# lingua-franca: заброшена (2020), её метаданные требуют python-dateutil~=2.6.0,
# что конфликтует с остальным стеком. На практике с dateutil 2.9 работает,
# поэтому ставим БЕЗ зависимостей:
#   pip install --no-deps -r requirements-optional.txt
lingua-franca==0.4.3
