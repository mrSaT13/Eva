# Eva Voice Assistant — production зависимости
# Установка: pip install -r requirements.txt
# Для тестов/документации: pip install -r requirements-dev.txt
# LLM-стек: версии langchain-* держать в sync с eva_plugin_llm/requirements.txt
# (Docker ставит оба файла вместе).

# --- Контент/знания ---
wikipedia-api~=0.5.8

# --- Аудио/речь ---
# pyttsx3: только локальный TTS, в Docker исключён (см. discover_plugins.yaml).
pyttsx3>=2.90,<3
vosk>=0.3.22
sounddevice~=0.4.4
soundfile~=0.12.1
edge-tts~=7.2.8
sherpa-onnx~=1.13.3

# --- Утилиты ---
requests~=2.32.4
# numpy без верхней границы: vosk>=0.3.22/sherpa-onnx/langchain дружат с numpy 2.x
numpy>=1.24
python-dateutil~=2.9.0
pyyaml~=6.0
# lingua-franca УБРАНА из prod: тянула python-dateutil~=2.6.0 и ломала резолв.
# eva.utils.all_num_to_text работает без неё (фолбэк на num_to_text_ru).
# Кому нужен именно lingua-franca: см. requirements-optional.txt (--no-deps).
httpx~=0.28.1

# --- Мессенджеры ---
pyTelegramBotAPI~=4.7.1

# --- Веб-сервер ---
uvicorn[standard]>=0.30.0
fastapi~=0.115.14
starlette~=0.46.2
websockets>=12.0

# --- LLM (дублируют eva_plugin_llm/requirements.txt — версии держать одинаковыми) ---
langchain-openai~=1.3.2
langchain-community~=0.4.2

# --- Консольный интерфейс (есть fallback-заглушка, можно убрать если не нужен) ---
aioconsole~=0.6.1
